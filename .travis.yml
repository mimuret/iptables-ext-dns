language: c
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y bind9 apt
install: sudo ./install-dependencies.sh
compiler: gcc
script:
  - ./autogen.sh
  - ./configure --libdir=/lib
  - make
  - sudo make install
  - sudo make check
