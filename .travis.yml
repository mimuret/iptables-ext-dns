language: c
before_install:
  - iptables-dev
install: sudo ./install-dependencies.sh
compiler: gcc
script:
  - ./autogen.sh
  - ./configure --libdir=/lib
  - make
  - sudo make install
  - sudo make check
